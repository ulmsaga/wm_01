<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.saga.wm.module.analysis">
    <!-- 공통 쿼리 예시 -->
    <select id="selectNow" resultType="string">
        SELECT NOW()
    </select>
</mapper>
